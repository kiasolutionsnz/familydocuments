import {createServer} from 'node:http';
import {readFile} from 'node:fs/promises';

const port=Number(process.env.FAKE_TELEGRAM_PORT||0);
const gateway=process.env.FP_GATEWAY_URL;
const secret=process.env.TELEGRAM_WEBHOOK_SECRET;
const token=process.env.TELEGRAM_BOT_TOKEN;
const fixtures=new Map();
if(process.env.FAKE_TELEGRAM_PDF)fixtures.set('synthetic-pdf',process.env.FAKE_TELEGRAM_PDF);
if(process.env.FAKE_TELEGRAM_JPEG)fixtures.set('synthetic-jpeg',process.env.FAKE_TELEGRAM_JPEG);
if(process.env.FAKE_TELEGRAM_PNG)fixtures.set('synthetic-png',process.env.FAKE_TELEGRAM_PNG);
const sent=[];
let nextUpdate=1000,nextMessage=1;

async function jsonBody(request,limit=128*1024){const chunks=[];let size=0;for await(const chunk of request){size+=chunk.length;if(size>limit)throw new Error('too large');chunks.push(chunk)}return JSON.parse(Buffer.concat(chunks).toString('utf8')||'{}')}
function reply(response,status,value,type='application/json'){response.writeHead(status,{'content-type':type,'cache-control':'no-store'});response.end(type==='application/json'?JSON.stringify(value):value)}
async function inject(value){const update={update_id:nextUpdate++,...value};const response=await fetch(`${gateway}/integrations/telegram/webhook`,{method:'POST',headers:{'content-type':'application/json','X-Telegram-Bot-Api-Secret-Token':secret},body:JSON.stringify(update)});return {status:response.status,body:await response.json().catch(()=>({}))}}

const server=createServer(async(request,response)=>{try{
  const url=new URL(request.url,'http://127.0.0.1');
  if(url.pathname==='/health')return reply(response,200,{status:'ok'});
  if(url.pathname==='/'){return reply(response,200,`<!doctype html><meta charset="utf-8"><title>Fake Telegram</title><style>body{font:16px system-ui;max-width:760px;margin:2rem auto;padding:1rem;color:#172033}input,select,button{font:inherit;padding:.7rem;margin:.3rem;border:1px solid #c8d1df;border-radius:.6rem}button{cursor:pointer;background:#eef4ff}.message{padding:.8rem;margin:.6rem 0;border:1px solid #dde3ec;border-radius:.8rem}.buttons{display:flex;flex-wrap:wrap}</style><h1>Disposable Fake Telegram</h1><p>Synthetic private messages only. Nothing contacts Telegram.</p><label>Test identity <select id=who><option value=70001>Owner</option><option value=70002>Read-only member</option><option value=70003>Multiple-Family member</option></select></label><div><input id=t aria-label="Message or command" placeholder="Message or command"><button onclick="sendText()">Send</button></div><div><button onclick="attach('synthetic-pdf')">Attach PDF</button><button onclick="attach('synthetic-png')">Attach PNG</button><button onclick="attach('synthetic-jpeg')">Attach JPEG</button></div><p id=status role=status></p><section id=messages aria-live=polite></section><script>
let seen=0;const identity=()=>Number(who.value);async function request(path,body){status.textContent='Sending...';const r=await fetch(path,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...body,user_id:identity(),chat_id:identity()})});status.textContent=r.ok?'Accepted for local processing.':'Local request failed.';await refresh()}
async function sendText(v=t.value){await request('/inject/text',{text:v});t.value=''}async function attach(file_id){await request('/inject/attachment',{file_id,caption:t.value});t.value=''}async function choose(nonce,message_id){await request('/inject/callback',{nonce,message_id})}
async function refresh(){const data=await fetch('/outbox').then(r=>r.json());messages.replaceChildren(...data.messages.map(item=>{const box=document.createElement('div');box.className='message';const text=document.createElement('div');text.textContent=item.text;box.append(text);const controls=document.createElement('div');controls.className='buttons';for(const row of item.reply_markup?.inline_keyboard||[])for(const option of row){const button=document.createElement('button');button.textContent=option.text;button.onclick=()=>choose(option.callback_data,item.message_id);controls.append(button)}box.append(controls);return box}));seen=data.messages.length}
setInterval(refresh,1500);refresh();const s=new URLSearchParams(location.search).get('start');if(s){t.value='/start '+s;status.textContent='Choose the matching synthetic identity, then select Send to connect.'}</script>`,'text/html');}
  if(url.pathname==='/inject/text'&&request.method==='POST'){const body=await jsonBody(request),user=Number(body.user_id||70001),chat=Number(body.chat_id||user);const result=await inject({message:{message_id:nextMessage++,text:String(body.text||'').slice(0,2000),chat:{id:chat,type:'private'},from:{id:user,first_name:'Synthetic',last_name:'Tester'}}});return reply(response,result.status,result.body)}
  if(url.pathname==='/inject/attachment'&&request.method==='POST'){const body=await jsonBody(request),user=Number(body.user_id||70001),chat=Number(body.chat_id||user);const fileId=String(body.file_id||'synthetic-pdf');if(!fixtures.has(fileId))return reply(response,422,{error:'unknown_fixture'});const mime=fileId.endsWith('jpeg')?'image/jpeg':fileId.endsWith('png')?'image/png':'application/pdf';const size=(await readFile(fixtures.get(fileId))).length;const result=await inject({message:{message_id:nextMessage++,caption:String(body.caption||'').slice(0,2000),chat:{id:chat,type:'private'},from:{id:user,first_name:'Synthetic',last_name:'Tester'},document:{file_id:fileId,file_unique_id:`unique-${fileId}`,file_name:`${fileId}.${mime==='application/pdf'?'pdf':mime==='image/png'?'png':'jpg'}`,mime_type:mime,file_size:size}}});return reply(response,result.status,result.body)}
  if(url.pathname==='/inject/replay'&&request.method==='POST'){const body=await jsonBody(request);const update={update_id:Number(body.update_id),message:{message_id:nextMessage++,text:String(body.text||'/help'),chat:{id:70001,type:'private'},from:{id:70001}}};const remote=await fetch(`${gateway}/integrations/telegram/webhook`,{method:'POST',headers:{'content-type':'application/json','X-Telegram-Bot-Api-Secret-Token':secret},body:JSON.stringify(update)});return reply(response,remote.status,await remote.json())}
  if(url.pathname==='/inject/callback'&&request.method==='POST'){const body=await jsonBody(request);const result=await inject({callback_query:{id:`callback-${nextUpdate}`,data:String(body.nonce||'').slice(0,100),from:{id:Number(body.user_id||70001)},message:{message_id:Number(body.message_id||1),chat:{id:Number(body.chat_id||70001),type:'private'}}}});return reply(response,result.status,result.body)}
  if(url.pathname==='/outbox')return reply(response,200,{messages:sent.map(({chat_id,text,reply_markup,message_id})=>({chat_id,text,reply_markup,message_id}))});
  if(url.pathname.includes(`/bot${token}/sendMessage`)){const body=await jsonBody(request);const item={...body,message_id:nextMessage++};sent.push(item);return reply(response,200,{ok:true,result:item})}
  if(url.pathname.includes(`/bot${token}/answerCallbackQuery`)){return reply(response,200,{ok:true,result:true})}
  if(url.pathname.includes(`/bot${token}/editMessageReplyMarkup`)){return reply(response,200,{ok:true,result:true})}
  if(url.pathname.includes(`/bot${token}/getFile`)){const body=await jsonBody(request);if(!fixtures.has(body.file_id))return reply(response,404,{ok:false});return reply(response,200,{ok:true,result:{file_path:`fixtures/${body.file_id}`}})}
  if(url.pathname.includes(`/file/bot${token}/fixtures/`)){const id=url.pathname.split('/').at(-1);const path=fixtures.get(id);if(!path)return reply(response,404,{ok:false});const bytes=await readFile(path);response.writeHead(200,{'content-type':'application/octet-stream','content-length':String(bytes.length)});return response.end(bytes)}
  return reply(response,404,{error:'not_found'});
}catch{return reply(response,400,{error:'invalid_request'})}});
server.listen(port,'127.0.0.1',()=>console.log(JSON.stringify({status:'ready',port:server.address().port})));
